.env
.env.local
.env.production
*.secret
node_modules/
build/
*.asc
*.pem
*.key

# VPS deploy secrets (deploy/.env, platform/.env) and local dump artifacts
deploy/.env
deploy/platform/.env
deploy/platform/backups/

# Test + tooling artifacts
coverage/
*.log
npm-debug.log*
.DS_Store
Thumbs.db

# Raw generated artwork drop-off. The shipped assets are the processed 512x512 PNGs in
# src/assets/categories/ (~450KB total); these originals are ~20MB and only needed to
# re-process at a different size. Keep them locally, out of git history.
images/
